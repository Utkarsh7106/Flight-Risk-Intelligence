# Copy to .env.local for local development (gitignored by Vite's default .gitignore).
# Backend must have this exact origin in its CORS_ORIGINS (see backend/.env.example) —
# it already defaults to http://localhost:5173, Vite's default dev port.
VITE_API_BASE_URL=http://localhost:8000
