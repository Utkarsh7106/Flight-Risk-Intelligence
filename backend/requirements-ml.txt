# Module 3 offline dataset generation + model training only — NOT a runtime
# dependency of the FastAPI app. Predictions/SHAP values are computed once
# by `python -m app.synthetic.generate` and `python -m app.synthetic.train`
# and persisted to Postgres; the live API just reads them. See
# backend/README.md ("Module 3's offline pipeline") and MODULE3_REFERENCE.md.
-r requirements.txt

numpy==2.3.4
scikit-learn==1.9.1
shap==0.51.0
